# 👨🏻‍🍳 Leftover Chef App (Fullstack CRUD)

ยินดีต้อนรับสู่ Leftover Chef โปรเจกต์ช่วยจัดการวัตถุดิบที่เหลือในตู้เย็นให้กลายเป็นเมนูสุดพิเศษ! โปรเจกต์นี้ถูกออกแบบด้วยโครงสร้าง Git Submodules เพื่อแยกการจัดการ Frontend และ Backend ออกจากกันอย่างเป็นระบบ

## 🏗 Project Structure

โปรเจกต์หลักนี้ทำหน้าที่เป็นตัวควบคุม (Orchestrator) สำหรับสองส่วนหลัก:

/frontend: พัฒนาด้วย Next.js (เชื่อมไปยัง [leftover-chef-app](https://github.com/Chafik-Rd/leftover-chef-app))

/backend: พัฒนาด้วย NestJS (เชื่อมไปยัง [leftover-chef-api](https://github.com/Chafik-Rd/leftover-chef-api))

## 🚀 Getting Started

1. การ Clone โปรเจกต์ (Clone with Submodules)
   เนื่องจากโปรเจกต์นี้ใช้ Git Submodules คุณต้องใช้คำสั่ง --recursive เพื่อดึงไฟล์ในโฟลเดอร์ย่อยออกมาด้วย:

```sh
git clone --recursive https://github.com/Chafik-Rd/leftover-chef.git
cd leftover-chef
```

💡 **Note:** หากคุณเผลอ Clone ไปแล้วแต่โฟลเดอร์ว่างเปล่า ให้ใช้คำสั่ง: git submodule update --init --recursive

2. การติดตั้ง (Installation)
คุณต้องทำการติดตั้ง Dependencies ของทั้งสองส่วน:

Frontend:
```sh
cd frontend
pnpm install
```
Backend:
```sh
cd ../backend
pnpm install
```

## ⚙️ Environment Configuration

ก่อนเริ่มรันโปรเจกต์ โปรดตั้งค่า Environment Variables ในแต่ละโฟลเดอร์:

- **Backend:** เข้าไปที่ `/backend` สร้างไฟล์ `.env` และตั้งค่าตาม `.env.example`
- **Frontend:** เข้าไปที่ `/frontend` สร้างไฟล์ `.env.local` และตั้งค่าตาม `.env.example`

## 🛠 How to Run
แนะนำให้เปิด Terminal แยกกันสองหน้าต่างเพื่อรันทั้งสองระบบพร้อมกัน:

หน้าต่างที่ 1: Backend (API)
 - Option A: Run with Docker (Recommended for Dev)
หากคุณมี Docker ติดตั้งอยู่แล้ว สามารถรันทั้ง Backend และ Database ได้ทันที:
    ```sh
    cd backend
    docker compose up --build
    ```
    ระบบจะเปิด API ที่พอร์ตตามที่คุณตั้งใน .env (Default: 3001) และรันฐานข้อมูล Postgres ให้โดยอัตโนมัติ

- Option B: Manual Run
หากต้องการรันแบบแยกส่วนด้วยตนเอง:
    ```sh
    cd backend
    pnpm run start:dev
    ```

หน้าต่างที่ 2: Frontend (Web)
```sh
cd frontend
pnpm run dev
```

## 🔄 Git Workflow สำหรับผู้พัฒนา
หากคุณมีการแก้ไขโค้ดในโฟลเดอร์ย่อย โปรดทำตามลำดับนี้เพื่อให้ Git ไม่คลาดเคลื่อน:

1. เข้าไปแก้ในโฟลเดอร์ย่อย (frontend หรือ backend)

2. Commit และ Push ในโฟลเดอร์ย่อยนั้นก่อน

3. กลับออกมาที่ Root (โฟลเดอร์หลัก) แล้วทำการ Commit เพื่ออัปเดต Pointer:

```sh
git add .
git commit -m "chore: update submodule pointers"
git push origin main
```